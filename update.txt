2018-10-18 增加自定义数据结构
	SQL：
		ALTER TABLE DS_FLOW
		ADD COLUMN DATATABLE VARCHAR(8000) NULL COMMENT '数据结构';
		
		ALTER TABLE DS_FLOW_TASK
		ADD COLUMN DATATABLE VARCHAR(10000) NULL COMMENT '数据结构';
		
		ALTER TABLE DS_FLOW_PI
		ADD COLUMN DATATABLE TEXT NULL COMMENT '数据结构';
		
		ALTER TABLE DS_FLOW_PI_WAITING
		ADD COLUMN DATATABLE TEXT NULL COMMENT '数据结构';
		
		ALTER TABLE DS_FLOW_PI_DATA
		ADD COLUMN DATATABLE TEXT NULL COMMENT '数据结构';


2018-10-18 增加多功能会签
	SQL：
		ALTER TABLE DS_FLOW_TASK
		ADD COLUMN SUBCOUNT int(4) NULL COMMENT '至少合并会签个数(不需要会签时值为-1)' AFTER TUSERS,
		ADD COLUMN SUBUSERS varchar(1000) NULL COMMENT '当前会签的用户ID(以逗号分隔可选用户，以|线分隔并行用户)' AFTER SUBCOUNT;
	
		ALTER TABLE DS_FLOW_PI_WAITING
		ADD COLUMN SUBCOUNT int(4) NULL COMMENT '至少合并会签个数(不需要会签时值为-1)' AFTER TUSERS,
		ADD COLUMN SUBUSERS varchar(1000) NULL COMMENT '已进行会签的用户ID(以逗号分隔用户)' AFTER SUBCOUNT;
